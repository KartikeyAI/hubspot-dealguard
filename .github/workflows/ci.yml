name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts
      - run: npm install --ignore-scripts --prefix src/app/cards
      - run: npm install --ignore-scripts --prefix src/app/settings
      - run: npm run check
      - name: Validate HubSpot manifests
        run: node -e "const fs=require('fs'); for (const f of ['src/app/app-hsmeta.json','src/app/webhooks/deal-events-hsmeta.json','src/app/workflow-actions/assess-deal-hsmeta.json']) JSON.parse(fs.readFileSync(f,'utf8'));"
      - run: npx wrangler d1 migrations apply dealguard-production --local
