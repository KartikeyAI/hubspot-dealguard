# Commercial Integrity observability

The optional enrichment records `success`, `latency_ms`, and `coverage_percent` metrics under `commercial_integrity_enrichment`. Authorization-required responses are valid product states, not operational failures. Source API failures are logged with portal and deal identifiers but without quote or line-item content.
