# Review guide

Review authorization gating first, then metadata allowlists and bounded association reads, followed by deterministic analysis and actions, Deal Brief weighting, UI behavior, and route isolation. Confirm that missing optional authorization creates no negative risk signal.
