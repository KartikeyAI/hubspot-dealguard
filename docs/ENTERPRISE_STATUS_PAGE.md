# Enterprise status-page requirements

The production status page must show current and historical state for the API, HubSpot webhooks, scheduled assessments, workflow actions, outbound notifications and billing processing. Incidents require timestamps, impact, updates and resolution. Do not publish availability percentages until external monitoring is operating.
