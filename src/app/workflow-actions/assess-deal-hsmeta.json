{
  "uid": "dealguard_assess_deal",
  "type": "workflow-action",
  "config": {
    "actionUrl": "https://dealguard-api.rokad.co/integrations/hubspot/workflow-actions/assess-deal",
    "isPublished": true,
    "supportedClients": [
      { "client": "WORKFLOWS" }
    ],
    "inputFields": [
      {
        "typeDefinition": {
          "name": "notifySlack",
          "type": "enumeration",
          "fieldType": "select",
          "options": [
            { "value": "no", "label": "Assess only" },
            { "value": "yes", "label": "Assess and notify Slack" }
          ]
        },
        "supportedValueTypes": ["STATIC_VALUE"],
        "isRequired": true
      }
    ],
    "outputFields": [
      { "typeDefinition": { "name": "readinessScore", "type": "number", "fieldType": "number" } },
      { "typeDefinition": { "name": "readinessStatus", "type": "string", "fieldType": "text" } },
      { "typeDefinition": { "name": "readinessGrade", "type": "string", "fieldType": "text" } },
      { "typeDefinition": { "name": "issueCount", "type": "number", "fieldType": "number" } },
      { "typeDefinition": { "name": "handoffStatus", "type": "string", "fieldType": "text" } },
      { "typeDefinition": { "name": "readinessSummary", "type": "string", "fieldType": "text" } },
      { "typeDefinition": { "name": "assessedAt", "type": "string", "fieldType": "text" } }
    ],
    "labels": {
      "en": {
        "appDisplayName": "DealGuard by Rokad",
        "actionName": "Assess deal with DealGuard",
        "actionDescription": "Runs configured readiness rules, returns reusable workflow outputs, and can optionally notify Slack.",
        "actionCardContent": "Assess deal; Slack notification: {{notifySlack}}",
        "inputFieldLabels": {
          "notifySlack": "Slack notification"
        },
        "inputFieldDescriptions": {
          "notifySlack": "Choose whether this workflow execution should also send the assessment to Slack."
        },
        "inputFieldOptionLabels": {
          "notifySlack": {
            "no": "Assess only",
            "yes": "Assess and notify Slack"
          }
        },
        "outputFieldLabels": {
          "readinessScore": "Readiness score",
          "readinessStatus": "Readiness status",
          "readinessGrade": "Readiness grade",
          "issueCount": "Issue count",
          "handoffStatus": "Handoff status",
          "readinessSummary": "Readiness summary",
          "assessedAt": "Assessed at"
        }
      }
    },
    "objectTypes": ["DEAL"]
  }
}
